import { ScrollToDirective } from './scrollto.directive';

describe('ScrolltoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToDirective();
    expect(directive).toBeTruthy();
  });
});
